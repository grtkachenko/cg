#pragma once

#include <iterator>
#include <boost/range/iterator.hpp>

namespace cg {
   namespace common
   {
      const double DOUBLE_MAX = std::numeric_limits<double>::max();
      const double DOUBLE_MIN = std::numeric_limits<double>::min();
      const double ILLEGAL_VERTEX = -1;
   }
}
